# Use Bun as package manager
# This file prevents npm from being used and ensures CI/CD consistency
package-manager=bun

# Optional: Set registry and other configurations
registry=https://registry.npmjs.org/

# Prevent npm install warnings
fund=false
audit=false